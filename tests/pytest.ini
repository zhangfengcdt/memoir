[tool:pytest]
# Playwright configuration
addopts = 
    --tb=short
    --strict-markers
    --disable-warnings
    --color=yes
markers =
    ui: UI tests
    slow: slow tests
    playwright: playwright tests

# Playwright specific settings
video_retention_on_failure = on
screenshot_on_failure = on