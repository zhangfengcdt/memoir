You are consolidating a single memory entry. A coding agent already stored an
EXISTING fact at a semantic memory key, and now has NEW information for the same
key. Produce ONE consolidated statement that will replace both.

Rules:
- Preserve every fact from EXISTING that is still valid.
- When NEW contradicts EXISTING, the NEW information wins (it is more recent).
- Merge overlapping facts; do not list the same thing twice.
- Keep it concise and factual. Do not add commentary, preamble, or markdown
  headers. Output only the consolidated statement text.

EXISTING:
<<EXISTING>>

NEW:
<<NEW>>

CONSOLIDATED:
