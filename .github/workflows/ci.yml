name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}

    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
        restore-keys: |
          ${{ runner.os }}-pip-

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run tests with pytest
      run: |
        pytest tests/ -v --cov=langmem_prollytree --cov-report=xml --cov-report=html

    - name: Upload coverage to Codecov
      if: matrix.python-version == '3.11'
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella

  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run Black (code formatting)
      run: |
        black --check --diff src/ tests/ examples/

    - name: Run Ruff (linting)
      run: |
        ruff check src/ tests/ examples/

    - name: Run isort (import sorting)
      run: |
        isort --check-only --diff src/ tests/ examples/

  type-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"

    - name: Run MyPy (type checking)
      run: |
        mypy src/langmem_prollytree --ignore-missing-imports

#  security:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v4
#
#    - name: Set up Python 3.11
#      uses: actions/setup-python@v4
#      with:
#        python-version: "3.11"
#
#    - name: Install dependencies
#      run: |
#        python -m pip install --upgrade pip
#        pip install bandit[toml] safety
#
#    - name: Run Bandit (security linting)
#      run: |
#        bandit -r src/ -f json -o bandit-report.json || true
#        bandit -r src/
#
#    - name: Run Safety (dependency vulnerability check)
#      run: |
#        safety check --json --output safety-report.json || true
#        safety check

#  build:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v4
#
#    - name: Set up Python 3.11
#      uses: actions/setup-python@v4
#      with:
#        python-version: "3.11"
#
#    - name: Install build dependencies
#      run: |
#        python -m pip install --upgrade pip
#        pip install build twine
#
#    - name: Build package
#      run: |
#        python -m build
#
#    - name: Check package
#      run: |
#        twine check dist/*
#
#    - name: Upload build artifacts
#      uses: actions/upload-artifact@v3
#      with:
#        name: dist
#        path: dist/
#
#  performance:
#    runs-on: ubuntu-latest
#    if: github.event_name == 'pull_request'
#    steps:
#    - uses: actions/checkout@v4
#
#    - name: Set up Python 3.11
#      uses: actions/setup-python@v4
#      with:
#        python-version: "3.11"
#
#    - name: Install dependencies
#      run: |
#        python -m pip install --upgrade pip
#        pip install -e ".[dev]"
#
#    - name: Run performance benchmarks
#      run: |
#        cd examples
#        timeout 300 python performance_benchmark.py || true
#
#    - name: Upload benchmark results
#      uses: actions/upload-artifact@v3
#      if: always()
#      with:
#        name: benchmark-results
#        path: examples/benchmark_results_*.json
#
#  docs:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v4
#
#    - name: Set up Python 3.11
#      uses: actions/setup-python@v4
#      with:
#        python-version: "3.11"
#
#    - name: Install dependencies
#      run: |
#        python -m pip install --upgrade pip
#        pip install -e ".[docs]"
#
#    - name: Test examples run without errors
#      run: |
#        cd examples
#        timeout 30 python -c "
#        # Test imports work
#        from langmem_prollytree import ProllyTreeMemoryStoreManager, get_taxonomy
#        print('✓ Imports successful')
#
#        # Test taxonomy
#        taxonomy = get_taxonomy()
#        stats = taxonomy.get_statistics()
#        print(f'✓ Taxonomy loaded with {stats[\"total_paths\"]} paths')
#
#        # Test basic functionality without external dependencies
#        from langmem_prollytree.taxonomy import OptimizedClassifier
#        classifier = OptimizedClassifier()
#        result = classifier.fast_classify('Test memory')
#        print(f'✓ Classification successful: {result.primary_path}')
#        "
#
#    - name: Check README links
#      run: |
#        # Simple check that README has expected sections
#        grep -q "Performance Improvements" README.md
#        grep -q "Quick Start" README.md
#        grep -q "Installation" README.md
#        echo "✓ README structure looks good"