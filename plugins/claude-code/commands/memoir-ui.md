---
description: "Launch (or re-open) the memoir web UI. Delegates to the memoir-ui skill."
---

Invoke the memoir-ui skill to launch or re-open the memoir web UI for the current project's store. If a UI is already running, the skill re-opens the existing browser tab instead of spawning a new server.
