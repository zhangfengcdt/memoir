kind: gate
hook: user-prompt-submit
description: "Domain noun 'memoir' fires even without an action verb"
env:
  USER_MEMORIES: "5"
  MEMOIR_CMD: "memoir"
prompt: "I'm curious about how the memoir branching model handles concurrent sessions."
expect:
  - kind: recall_block_emitted
